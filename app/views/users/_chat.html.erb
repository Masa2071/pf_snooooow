  <!--< % if (current_user.followed_by? @user) && (@user.followed_by? current_user)  %>--><!--相互フォローの場合-->
<% if isRoom == true %><!-- 既存のチャットがある時-->
  <%= link_to room_path(roomId)  do %>
    <i class="far fa-envelope message-icon"></i>
  <% end %>
<% else %>
  <%= form_with model: room, local: true, class: 'message-form' do |f| %>
    <%= fields_for entry do |e| %>
    <%= e.hidden_field :user_id, value: user.id %>
    <% end %>
    <%= button_tag type: 'submit', class: 'btn' do %>
      <i class="far fa-envelope message-icon"></i>
    <% end %>
  <% end %>
<% end %>

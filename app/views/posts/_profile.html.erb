<%= link_to user_path(post.user_id), class: "btn" do %>
  <%= attachment_image_tag post.user, :profile_image, size: "70x70", fallback: 'no_image.jpg', format: 'jpeg', class: "rounded-circle" %>
  <span class="profile-name-position ml-3"><%= post.user.name %></span>
<% end %>